#define REG_COLOR r0

smile:
	set8 ${REG_COLOR} 255     ; Sets color register to 255 (alpha 100%)
	store8 ${REG_COLOR} 8274  ; Draws alpha for part of smile face
	store8 ${REG_COLOR} 8278  ; Draws alpha for part of smile face
	store8 ${REG_COLOR} 8353  ; Draws alpha for part of smile face
	store8 ${REG_COLOR} 8359  ; Draws alpha for part of smile face
	store8 ${REG_COLOR} 8393  ; Draws alpha for part of smile face
	store8 ${REG_COLOR} 8399  ; Draws alpha for part of smile face
	store8 ${REG_COLOR} 8434  ; Draws alpha for part of smile face
	store8 ${REG_COLOR} 8438  ; Draws alpha for part of smile face
	store8 ${REG_COLOR} 8475  ; Draws alpha for part of smile face
	store8 ${REG_COLOR} 8476  ; Draws alpha for part of smile face
	store8 ${REG_COLOR} 8477  ; Draws alpha for part of smile face

forever:         ; Automatically jumped to after smile face is drawn (due to label ordering)
	jmp forever  ; Restarts forever loop
